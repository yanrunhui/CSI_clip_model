diff --git a/data/caption.py b/data/caption.py
index 5d4a12c..3e4500b 100644
--- a/data/caption.py
+++ b/data/caption.py
@@ -8,7 +8,7 @@ from .semantic_key import SemanticKey, semantic_field_choices
 
 PROP_S1 = [
     "{env} environment with {los} propagation and {richness} multipath.",
-    "{los} channel in an {env} setting with {richness} resolvable paths.",
+    "{los} channel in an {env} setting with {richness} effective multipath complexity.",
     "{env}, {los}, {richness} multipath components.",
 ]
 
@@ -30,9 +30,9 @@ FOCUSED_PROP_TEMPLATES = {
         "Propagation condition: {los}.",
     ],
     "n_paths": [
-        "The channel has {richness} multipath components.",
+        "The channel shows {richness} effective multipath complexity.",
         "Multipath richness is {richness}.",
-        "Resolvable path count is {richness}.",
+        "Effective path complexity is {richness}.",
     ],
     "delay_spread": [
         "The delay spread is {ds}.",
diff --git a/data/dataset.py b/data/dataset.py
index cda012c..daea952 100644
--- a/data/dataset.py
+++ b/data/dataset.py
@@ -8,11 +8,12 @@ from torch.utils.data import Dataset
 
 from .caption import CaptionGenerator
 from .semantic_key import (
-    PROP_DISC,
+    DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     SemanticKey,
-    discretize,
+    legacy_path_richness_from_n_paths,
     normalize_k_factor_bin,
     normalize_path_richness,
+    semantic_path_richness_source_choices,
 )
 from .tokenizer import CaptionTokenizer
 
@@ -74,7 +75,9 @@ class PreprocessedSample:
     obs_bw_label: int
     prop_caption: str
     instance_caption: str = ""
+    aux_semantic_key: SemanticKey | None = None
     n_paths: int = 0
+    effective_path_count: int | None = None
     delay_spread_s: float = 0.0
     azimuth_spread_deg: float = 0.0
     k_factor_db: float = 0.0
@@ -87,7 +90,9 @@ class PreprocessedSample:
 
 def _ensure_continuous_fields(sample: PreprocessedSample) -> PreprocessedSample:
     defaults = {
+        "aux_semantic_key": None,
         "n_paths": 0,
+        "effective_path_count": None,
         "delay_spread_s": 0.0,
         "azimuth_spread_deg": 0.0,
         "k_factor_db": 0.0,
@@ -166,14 +171,21 @@ def semantic_key_for_mode(
     key: SemanticKey,
     mode: str,
     n_paths: int | None = None,
+    effective_path_count: int | None = None,
+    path_richness_source: str = DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
 ) -> SemanticKey:
     if mode == "full":
         return key
-    path_richness = (
-        normalize_path_richness(discretize(float(n_paths), PROP_DISC["n_paths"]))
-        if n_paths is not None
-        else normalize_path_richness(key.path_richness)
-    )
+    del effective_path_count
+    if path_richness_source not in semantic_path_richness_source_choices():
+        raise ValueError(
+            f"Unsupported semantic path_richness source={path_richness_source!r}. "
+            f"Choose from: {', '.join(semantic_path_richness_source_choices())}"
+        )
+    if path_richness_source == "legacy_n_paths" and n_paths is not None:
+        path_richness = legacy_path_richness_from_n_paths(n_paths)
+    else:
+        path_richness = normalize_path_richness(key.path_richness)
     if mode in {
         "coarse",
         "coarse_delay",
@@ -201,16 +213,28 @@ def semantic_key_for_mode(
     )
 
 
-def apply_semantic_key_mode(samples: list[PreprocessedSample], mode: str) -> list[PreprocessedSample]:
+def apply_semantic_key_mode(
+    samples: list[PreprocessedSample],
+    mode: str,
+    path_richness_source: str = DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
+) -> list[PreprocessedSample]:
     if mode == "full":
         return samples
     generator = CaptionGenerator()
     remapped = []
     for sample in samples:
-        key = semantic_key_for_mode(sample.semantic_key, mode, n_paths=sample.n_paths)
+        source_key = sample.aux_semantic_key or sample.semantic_key
+        key = semantic_key_for_mode(
+            source_key,
+            mode,
+            n_paths=sample.n_paths,
+            effective_path_count=getattr(sample, "effective_path_count", None),
+            path_richness_source=path_richness_source,
+        )
         remapped_sample = replace(
             sample,
             semantic_key=key,
+            aux_semantic_key=source_key,
             prop_caption=generator.generate(key),
         )
         remapped_sample = replace(
@@ -308,6 +332,7 @@ def collate_fn(
         "physics_raw_targets": physics_raw_targets,
         "instance_captions": [sample.instance_caption for sample in batch],
         "semantic_keys": [sample.semantic_key for sample in batch],
+        "aux_semantic_keys": [sample.aux_semantic_key or sample.semantic_key for sample in batch],
         "group_ids": [sample.group_id for sample in batch],
         "config_keys": [sample.config_key for sample in batch],
     }
@@ -335,6 +360,7 @@ def build_synthetic_samples(
         tokens = torch.randn(n_tokens, 8, 128)
         beam_positions = torch.rand(n_tokens, 2)
         caption = caption_generator.generate(key)
+        effective_path_count = max(idx % 8, 1)
         instance_caption = caption_generator.generate_instance(
             key,
             n_paths=max(idx % 8, 1),
@@ -368,6 +394,7 @@ def build_synthetic_samples(
                 prop_caption=caption,
                 instance_caption=instance_caption,
                 n_paths=max(idx % 8, 1),
+                effective_path_count=effective_path_count,
                 delay_spread_s=float((idx % 3 + 1) * 50e-9),
                 azimuth_spread_deg=float((idx % 3 + 1) * 15.0),
                 k_factor_db=float(12.0 - (idx % 3) * 5.0),
diff --git a/data/semantic_key.py b/data/semantic_key.py
index 9996708..a30dd3a 100644
--- a/data/semantic_key.py
+++ b/data/semantic_key.py
@@ -10,6 +10,9 @@ PATH_RICHNESS_LABELS = (
     "moderate",
     "high",
 )
+EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB = 20.0
+DEFAULT_PATH_RICHNESS_NUM_BINS = 3
+DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE = "current"
 
 PROP_DISC = {
     "n_paths": {
@@ -98,7 +101,11 @@ SEMANTIC_FIELD_KEY_FIELDS = {
 }
 
 SEMANTIC_KEY_FIELDS = tuple(SemanticKey.__dataclass_fields__)
-DERIVED_SEMANTIC_KEY_FIELDS = ("k_factor_binary", "first_power_binary")
+DERIVED_SEMANTIC_KEY_FIELDS = (
+    "k_factor_binary",
+    "first_power_binary",
+    "effective_path_richness",
+)
 
 DEFAULT_ATTRIBUTE_FIELDS = (
     "los_status",
@@ -126,11 +133,55 @@ def normalize_path_richness(value: str) -> str:
     return "high"
 
 
+def semantic_path_richness_source_choices() -> tuple[str, str]:
+    return ("current", "legacy_n_paths")
+
+
+def path_richness_num_bin_choices() -> tuple[int, int]:
+    return (2, 3)
+
+
+def path_richness_from_count(
+    path_count: int | float,
+    num_bins: int = DEFAULT_PATH_RICHNESS_NUM_BINS,
+) -> str:
+    if num_bins not in path_richness_num_bin_choices():
+        raise ValueError(
+            f"Unsupported path_richness num_bins={num_bins!r}. "
+            f"Choose from: {', '.join(str(choice) for choice in path_richness_num_bin_choices())}"
+        )
+    try:
+        count = int(path_count)
+    except (TypeError, ValueError):
+        return "low"
+    if count <= 1:
+        return "low"
+    if num_bins == 2:
+        return "high"
+    if count <= 3:
+        return "moderate"
+    return "high"
+
+
 def normalize_k_factor_bin(value: str) -> str:
     return "weak" if str(value) == "weak" else "strong"
 
 
-def semantic_key_attribute_raw_value(key: SemanticKey, field: str) -> str:
+def legacy_path_richness_from_n_paths(n_paths: int | float) -> str:
+    try:
+        return normalize_path_richness(discretize(float(n_paths), PROP_DISC["n_paths"]))
+    except (TypeError, ValueError):
+        return "low"
+
+
+def semantic_key_attribute_raw_value_from_sources(
+    key: SemanticKey,
+    field: str,
+    aux_key: SemanticKey | None = None,
+) -> str:
+    if field == "effective_path_richness":
+        source_key = aux_key if aux_key is not None else key
+        return normalize_path_richness(source_key.path_richness)
     if field == "path_richness":
         return normalize_path_richness(key.path_richness)
     if field == "k_factor_bin":
@@ -142,12 +193,29 @@ def semantic_key_attribute_raw_value(key: SemanticKey, field: str) -> str:
     return str(getattr(key, field))
 
 
-def semantic_key_attribute_value(
+def semantic_key_attribute_raw_value(key: SemanticKey, field: str) -> str:
+    return semantic_key_attribute_raw_value_from_sources(key, field, aux_key=None)
+
+
+def semantic_sample_attribute_raw_value(
+    sample: object,
+    field: str,
+) -> str:
+    aux_key = getattr(sample, "aux_semantic_key", None)
+    return semantic_key_attribute_raw_value_from_sources(
+        getattr(sample, "semantic_key"),
+        field,
+        aux_key=aux_key,
+    )
+
+
+def semantic_key_attribute_value_from_sources(
     key: SemanticKey,
     field: str,
     attribute_remap: AttributeRemap | None = None,
+    aux_key: SemanticKey | None = None,
 ) -> str:
-    raw_value = semantic_key_attribute_raw_value(key, field)
+    raw_value = semantic_key_attribute_raw_value_from_sources(key, field, aux_key=aux_key)
     field_remap = (attribute_remap or {}).get(field, {})
     for mapped_value, source_values in field_remap.items():
         if raw_value in set(source_values):
@@ -155,6 +223,33 @@ def semantic_key_attribute_value(
     return raw_value
 
 
+def semantic_key_attribute_value(
+    key: SemanticKey,
+    field: str,
+    attribute_remap: AttributeRemap | None = None,
+) -> str:
+    return semantic_key_attribute_value_from_sources(
+        key,
+        field,
+        attribute_remap=attribute_remap,
+        aux_key=None,
+    )
+
+
+def semantic_sample_attribute_value(
+    sample: object,
+    field: str,
+    attribute_remap: AttributeRemap | None = None,
+) -> str:
+    aux_key = getattr(sample, "aux_semantic_key", None)
+    return semantic_key_attribute_value_from_sources(
+        getattr(sample, "semantic_key"),
+        field,
+        attribute_remap=attribute_remap,
+        aux_key=aux_key,
+    )
+
+
 def implied_attribute_value_filters(
     fields: tuple[str, ...],
     attribute_remap: AttributeRemap | None = None,
@@ -225,12 +320,77 @@ def _get(sample: Mapping[str, float] | object, key: str, default=None):
     return getattr(sample, key, default)
 
 
-def build_semantic_key(sample: Mapping[str, float] | object) -> SemanticKey:
+def effective_path_count_from_power_dbw(
+    power_dbw: np.ndarray | list[float] | tuple[float, ...],
+    relative_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+) -> int:
+    power_array = np.asarray(power_dbw, dtype=float).reshape(-1)
+    finite = power_array[np.isfinite(power_array)]
+    if finite.size == 0:
+        return 0
+    max_power = float(finite.max())
+    threshold = max_power - float(relative_threshold_db)
+    return int(np.sum(finite >= threshold))
+
+
+def infer_effective_path_count(
+    sample: Mapping[str, float] | object,
+    relative_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+) -> int:
+    direct_value = _get(sample, "effective_path_count")
+    if direct_value is not None:
+        try:
+            return max(int(direct_value), 0)
+        except (TypeError, ValueError):
+            pass
+
+    for key in (
+        "power_dbw",
+        "path_power_dbw",
+        "path_powers_dbw",
+        "ray_power_dbw",
+        "ray_powers_dbw",
+        "power",
+    ):
+        value = _get(sample, key)
+        if value is None:
+            continue
+        return effective_path_count_from_power_dbw(
+            value,
+            relative_threshold_db=relative_threshold_db,
+        )
+
+    fallback_paths = _get(sample, "n_paths", 0)
+    try:
+        return max(int(fallback_paths), 0)
+    except (TypeError, ValueError):
+        return 0
+
+
+def infer_path_richness(
+    sample: Mapping[str, float] | object,
+    num_bins: int = DEFAULT_PATH_RICHNESS_NUM_BINS,
+    relative_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+) -> str:
+    return path_richness_from_count(
+        infer_effective_path_count(sample, relative_threshold_db=relative_threshold_db),
+        num_bins=num_bins,
+    )
+
+
+def build_semantic_key(
+    sample: Mapping[str, float] | object,
+    *,
+    path_richness_num_bins: int = DEFAULT_PATH_RICHNESS_NUM_BINS,
+    effective_path_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+) -> SemanticKey:
     return SemanticKey(
         env_type=_get(sample, "environment_type"),
         los_status="los" if bool(_get(sample, "los_status")) else "nlos",
-        path_richness=normalize_path_richness(
-            discretize(float(_get(sample, "n_paths")), PROP_DISC["n_paths"])
+        path_richness=infer_path_richness(
+            sample,
+            num_bins=path_richness_num_bins,
+            relative_threshold_db=effective_path_threshold_db,
         ),
         ds_bin=discretize(
             float(_get(sample, "delay_spread")) * 1e9,
diff --git a/scripts/evaluate.py b/scripts/evaluate.py
index 619aedb..4a102df 100644
--- a/scripts/evaluate.py
+++ b/scripts/evaluate.py
@@ -25,12 +25,15 @@ from data.dataset import (
     semantic_key_mode_choices,
 )
 from data.semantic_key import (
+    DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     SemanticKey,
     default_attribute_fields,
     implied_attribute_value_filters,
-    semantic_key_attribute_raw_value,
-    semantic_key_attribute_value,
+    semantic_path_richness_source_choices,
+    semantic_key_attribute_value_from_sources,
     semantic_key_field_choices,
+    semantic_sample_attribute_raw_value,
+    semantic_sample_attribute_value,
 )
 from data.tokenizer import CaptionTokenizer
 from models.encoder import CSIEncoder
@@ -126,6 +129,22 @@ def _infer_semantic_key_mode(checkpoint: dict | None, override: str | None) -> s
     return "full"
 
 
+def _infer_semantic_key_path_richness_source(
+    checkpoint: dict | None,
+    override: str | None,
+) -> str:
+    if override is not None:
+        return override
+    if checkpoint is not None:
+        return str(
+            checkpoint.get("args", {}).get(
+                "semantic_key_path_richness_source",
+                DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
+            )
+        )
+    return DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE
+
+
 def _infer_limit_samples(checkpoint: dict | None, override: int | None) -> int | None:
     if override is not None:
         return override
@@ -277,7 +296,7 @@ def build_attribute_label_maps(
     for field in fields:
         values = sorted(
             {
-                semantic_key_attribute_value(sample.semantic_key, field, attribute_remap)
+                semantic_sample_attribute_value(sample, field, attribute_remap)
                 for sample in samples
             }
         )
@@ -317,7 +336,7 @@ def filter_samples_by_attribute_values(samples, filters: dict[str, tuple[str, ..
         next_filtered = [
             sample
             for sample in filtered
-            if semantic_key_attribute_raw_value(sample.semantic_key, field) in allowed_values
+            if semantic_sample_attribute_raw_value(sample, field) in allowed_values
         ]
         if not next_filtered:
             raise ValueError(
@@ -355,7 +374,7 @@ def limit_samples_by_attribute_value_for_debug(
     grouped = {}
     for sample in samples:
         grouped.setdefault(
-            semantic_key_attribute_value(sample.semantic_key, attribute_field, attribute_remap),
+            semantic_sample_attribute_value(sample, attribute_field, attribute_remap),
             [],
         ).append(sample)
     limited = []
@@ -378,6 +397,7 @@ def evaluate(
     text_mode_override: str | None = None,
     min_class_size_override: int | None = None,
     semantic_key_mode_override: str | None = None,
+    semantic_key_path_richness_source_override: str | None = None,
     attribute_fields_override: tuple[str, ...] | None = None,
     filter_attribute_values_override: dict[str, tuple[str, ...]] | None = None,
     limit_samples_override: int | None = None,
@@ -390,6 +410,10 @@ def evaluate(
     text_mode = _infer_text_mode(checkpoint, text_mode_override)
     min_class_size = _infer_min_class_size(checkpoint, min_class_size_override)
     semantic_key_mode = _infer_semantic_key_mode(checkpoint, semantic_key_mode_override)
+    semantic_key_path_richness_source = _infer_semantic_key_path_richness_source(
+        checkpoint,
+        semantic_key_path_richness_source_override,
+    )
     attribute_fields = _infer_attribute_fields(checkpoint, attribute_fields_override)
     attribute_remap = _infer_attribute_remap(checkpoint)
     filter_attribute_values = _infer_filter_attribute_values(
@@ -409,7 +433,11 @@ def evaluate(
         checkpoint,
         limit_samples_per_attribute_value_override,
     )
-    mode_samples = apply_semantic_key_mode(dataset.samples, semantic_key_mode)
+    mode_samples = apply_semantic_key_mode(
+        dataset.samples,
+        semantic_key_mode,
+        path_richness_source=semantic_key_path_richness_source,
+    )
     if semantic_key_mode != "full":
         before_counts = Counter(sample.semantic_key for sample in dataset.samples)
         after_counts = Counter(sample.semantic_key for sample in mode_samples)
@@ -435,7 +463,7 @@ def evaluate(
                 f"{value}:{count}"
                 for value, count in sorted(
                     Counter(
-                        semantic_key_attribute_raw_value(sample.semantic_key, field)
+                        semantic_sample_attribute_raw_value(sample, field)
                         for sample in value_filtered_samples
                     ).items()
                 )
@@ -460,8 +488,8 @@ def evaluate(
         before_counts = Counter(sample.semantic_key for sample in samples)
         after_counts = Counter(sample.semantic_key for sample in balanced_limited_samples)
         value_counts = Counter(
-            semantic_key_attribute_value(
-                sample.semantic_key,
+            semantic_sample_attribute_value(
+                sample,
                 limit_samples_by_attribute,
                 attribute_remap,
             )
@@ -563,8 +591,18 @@ def evaluate(
             for field, label_map in attribute_label_maps.items():
                 all_attribute_logits[field].append(attribute_logits[field].cpu())
                 all_attribute_labels[field].extend(
-                    label_map[semantic_key_attribute_value(key, field, attribute_remap)]
-                    for key in batch["semantic_keys"]
+                    label_map[
+                        semantic_key_attribute_value_from_sources(
+                            key,
+                            field,
+                            attribute_remap=attribute_remap,
+                            aux_key=aux_key,
+                        )
+                    ]
+                    for key, aux_key in zip(
+                        batch["semantic_keys"],
+                        batch.get("aux_semantic_keys", batch["semantic_keys"]),
+                    )
                 )
         physics_predictions = model.predict_physics(csi_features_raw)
         all_physics_predictions.append(physics_predictions.cpu())
@@ -644,6 +682,7 @@ def evaluate(
     print(f"logit_scale={logit_scale:.4f}")
     print(f"text_mode={text_mode}")
     print(f"semantic_key_mode={semantic_key_mode}")
+    print(f"semantic_key_path_richness_source={semantic_key_path_richness_source}")
     print(f"min_class_size={min_class_size}")
     print(f"attribute_remap={format_attribute_remap(attribute_remap)}")
     print(f"filter_attribute_values={format_attribute_value_filters(filter_attribute_values)}")
@@ -1119,6 +1158,11 @@ def main() -> None:
         type=int,
         help="Drop semantic classes with fewer than this many samples before evaluation. Defaults to checkpoint args.",
     )
+    parser.add_argument(
+        "--semantic-key-path-richness-source",
+        choices=semantic_path_richness_source_choices(),
+        help="Which path_richness source to use when remapping semantic_key_mode prototypes. Defaults to checkpoint args.",
+    )
     parser.add_argument(
         "--filter-attribute-values",
         action="append",
@@ -1166,6 +1210,7 @@ def main() -> None:
         text_mode_override=args.text_mode,
         min_class_size_override=args.min_class_size,
         semantic_key_mode_override=args.semantic_key_mode,
+        semantic_key_path_richness_source_override=args.semantic_key_path_richness_source,
         attribute_fields_override=tuple(args.attribute_classifier_fields) if args.attribute_classifier_fields else None,
         filter_attribute_values_override=(
             parse_attribute_value_filters(args.filter_attribute_values)
diff --git a/scripts/make_balanced_subset.py b/scripts/make_balanced_subset.py
index a1bc9e8..17795f3 100644
--- a/scripts/make_balanced_subset.py
+++ b/scripts/make_balanced_subset.py
@@ -14,7 +14,10 @@ if str(ROOT) not in sys.path:
     sys.path.insert(0, str(ROOT))
 
 from data.dataset import apply_semantic_key_mode, semantic_key_mode_choices
-from data.semantic_key import semantic_key_attribute_value, semantic_key_field_choices
+from data.semantic_key import (
+    semantic_key_field_choices,
+    semantic_sample_attribute_value,
+)
 
 
 def load_config(path: str | None) -> dict:
@@ -160,6 +163,7 @@ FIELD_ALIASES = {
     "first_angle": "first_angle_bin",
     "reflection": "reflection_bin",
     "diffraction": "diffraction_bin",
+    "effective_path": "effective_path_richness",
 }
 
 
@@ -202,7 +206,7 @@ def _balance_key(
         if attr == "semantic_key":
             raise ValueError("semantic_key cannot be combined with other balance fields.")
         if attr in semantic_key_field_choices():
-            values.append((attr, semantic_key_attribute_value(sample.semantic_key, attr, attribute_remap)))
+            values.append((attr, semantic_sample_attribute_value(sample, attr, attribute_remap)))
             continue
         if not hasattr(sample.semantic_key, attr):
             raise ValueError(f"Unknown SemanticKey field for balancing: {field}")
diff --git a/scripts/preprocess_all.py b/scripts/preprocess_all.py
index 291c363..f402d26 100644
--- a/scripts/preprocess_all.py
+++ b/scripts/preprocess_all.py
@@ -21,7 +21,13 @@ if str(ROOT) not in sys.path:
 from data.caption import CaptionGenerator
 from data.dataset import PreprocessedSample
 from data.preprocess import preprocess_sample
-from data.semantic_key import SemanticKey
+from data.semantic_key import (
+    DEFAULT_PATH_RICHNESS_NUM_BINS,
+    EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+    SemanticKey,
+    effective_path_count_from_power_dbw,
+    path_richness_num_bin_choices,
+)
 
 PROP_MAGIC = {b"PORP", b"PROP"}
 PROP_HEADER_BYTES = 32
@@ -960,6 +966,7 @@ def extract_semantic_observables_from_deepmimo(
     aoa_az_deg: np.ndarray,
     power_dbw: np.ndarray,
     inter_code: np.ndarray,
+    effective_path_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
 ) -> dict[str, float | int | str]:
     valid = _path_mask(power_dbw, delay_s)
     n_valid_paths = int(valid.sum())
@@ -998,11 +1005,16 @@ def extract_semantic_observables_from_deepmimo(
         else 0.0
     )
     k_factor_db = estimate_k_factor_db(power_dbw, los_value)
+    effective_path_count = effective_path_count_from_power_dbw(
+        power_dbw,
+        relative_threshold_db=effective_path_threshold_db,
+    )
 
     return {
         "environment_type": env_type or infer_env_type(scenario),
         "los_status": int(los_value == 1),
         "n_paths": max(int(num_paths_value), n_valid_paths),
+        "effective_path_count": effective_path_count,
         "delay_spread": delay_spread,
         "azimuth_spread_aoa": np.deg2rad(azimuth_spread_deg),
         "azimuth_spread_deg": azimuth_spread_deg,
@@ -1024,6 +1036,8 @@ def build_semantic_key_from_deepmimo(
     aoa_az_deg: np.ndarray,
     power_dbw: np.ndarray,
     inter_code: np.ndarray,
+    path_richness_num_bins: int = DEFAULT_PATH_RICHNESS_NUM_BINS,
+    effective_path_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
 ) -> SemanticKey:
     observables = extract_semantic_observables_from_deepmimo(
         scenario=scenario,
@@ -1034,11 +1048,16 @@ def build_semantic_key_from_deepmimo(
         aoa_az_deg=aoa_az_deg,
         power_dbw=power_dbw,
         inter_code=inter_code,
+        effective_path_threshold_db=effective_path_threshold_db,
     )
 
     from data.semantic_key import build_semantic_key
 
-    return build_semantic_key(observables)
+    return build_semantic_key(
+        observables,
+        path_richness_num_bins=path_richness_num_bins,
+        effective_path_threshold_db=effective_path_threshold_db,
+    )
 
 
 def derive_config_info(dataset) -> tuple[str, int, int]:
@@ -1078,6 +1097,8 @@ def preprocess_deepmimo_dataset(
     patch_2d: tuple[int, int],
     target_nf: int,
     include_empty_samples: bool,
+    path_richness_num_bins: int = DEFAULT_PATH_RICHNESS_NUM_BINS,
+    effective_path_threshold_db: float = EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
 ) -> list[PreprocessedSample]:
     caption_generator = CaptionGenerator()
     array_type, n_row, n_col = derive_config_info(dataset)
@@ -1126,10 +1147,15 @@ def preprocess_deepmimo_dataset(
             aoa_az_deg=np.asarray(dataset.aoa_az[idx]).reshape(-1),
             power_dbw=power_dbw,
             inter_code=np.asarray(dataset.inter[idx]).reshape(-1),
+            effective_path_threshold_db=effective_path_threshold_db,
         )
         from data.semantic_key import build_semantic_key
 
-        semantic_key = build_semantic_key(observables)
+        semantic_key = build_semantic_key(
+            observables,
+            path_richness_num_bins=path_richness_num_bins,
+            effective_path_threshold_db=effective_path_threshold_db,
+        )
         instance_caption = caption_generator.generate_instance(
             semantic_key,
             n_paths=int(observables["n_paths"]),
@@ -1167,6 +1193,7 @@ def preprocess_deepmimo_dataset(
                 prop_caption=caption_generator.generate(semantic_key),
                 instance_caption=instance_caption,
                 n_paths=int(observables["n_paths"]),
+                effective_path_count=int(observables["effective_path_count"]),
                 delay_spread_s=float(observables["delay_spread"]),
                 azimuth_spread_deg=float(observables["azimuth_spread_deg"]),
                 k_factor_db=float(observables["k_factor_db"]),
@@ -1217,6 +1244,19 @@ def main() -> None:
     parser.add_argument("--target-nf", type=int, default=128)
     parser.add_argument("--patch-1d", type=int, default=4)
     parser.add_argument("--patch-2d", type=int, nargs=2, default=[2, 2])
+    parser.add_argument(
+        "--path-richness-bins",
+        type=int,
+        choices=path_richness_num_bin_choices(),
+        default=DEFAULT_PATH_RICHNESS_NUM_BINS,
+        help="Number of effective path-richness bins. Use 2 for low/high or 3 for low/moderate/high.",
+    )
+    parser.add_argument(
+        "--effective-path-threshold-db",
+        type=float,
+        default=EFFECTIVE_PATH_RELATIVE_THRESHOLD_DB,
+        help="Relative dB threshold below the strongest path used to count effective paths.",
+    )
     parser.add_argument(
         "--include-empty-samples",
         action="store_true",
@@ -1287,6 +1327,8 @@ def main() -> None:
         patch_2d=tuple(args.patch_2d),
         target_nf=args.target_nf,
         include_empty_samples=args.include_empty_samples,
+        path_richness_num_bins=args.path_richness_bins,
+        effective_path_threshold_db=args.effective_path_threshold_db,
     )
 
     output_path = Path(args.output)
diff --git a/scripts/pretrain.py b/scripts/pretrain.py
index eca7726..31714fa 100644
--- a/scripts/pretrain.py
+++ b/scripts/pretrain.py
@@ -28,12 +28,14 @@ from data.dataset import (
     semantic_key_mode_choices,
 )
 from data.semantic_key import (
+    DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     SemanticKey,
     default_attribute_fields,
     implied_attribute_value_filters,
-    semantic_key_attribute_raw_value,
-    semantic_key_attribute_value,
+    semantic_path_richness_source_choices,
     semantic_key_field_choices,
+    semantic_sample_attribute_raw_value,
+    semantic_sample_attribute_value,
 )
 from data.tokenizer import CaptionTokenizer
 from models.encoder import CSIEncoder
@@ -303,13 +305,13 @@ def build_attribute_banks(
     for field in fields:
         values = sorted(
             {
-                semantic_key_attribute_value(sample.semantic_key, field, attribute_remap)
+                semantic_sample_attribute_value(sample, field, attribute_remap)
                 for sample in samples
             }
         )
         label_map = {value: idx for idx, value in enumerate(values)}
         counts = Counter(
-            semantic_key_attribute_value(sample.semantic_key, field, attribute_remap)
+            semantic_sample_attribute_value(sample, field, attribute_remap)
             for sample in samples
         )
         label_maps[field] = label_map
@@ -499,7 +501,7 @@ def filter_samples_by_attribute_values(samples, filters: dict[str, tuple[str, ..
         next_filtered = [
             sample
             for sample in filtered
-            if semantic_key_attribute_raw_value(sample.semantic_key, field) in allowed_values
+            if semantic_sample_attribute_raw_value(sample, field) in allowed_values
         ]
         if not next_filtered:
             raise ValueError(
@@ -537,7 +539,7 @@ def limit_samples_by_attribute_value_for_debug(
     grouped = {}
     for sample in samples:
         grouped.setdefault(
-            semantic_key_attribute_value(sample.semantic_key, attribute_field, attribute_remap),
+            semantic_sample_attribute_value(sample, attribute_field, attribute_remap),
             [],
         ).append(sample)
     limited = []
@@ -554,12 +556,17 @@ def limit_samples_by_attribute_value_for_debug(
 def build_demo_components(
     device: torch.device,
     semantic_key_mode: str = "full",
+    semantic_key_path_richness_source: str = DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     attribute_fields: tuple[str, ...] = (),
     attribute_remap: dict[str, dict[str, tuple[str, ...]]] | None = None,
 ):
     caption_generator = CaptionGenerator()
     samples = build_synthetic_samples(128, caption_generator=caption_generator)
-    samples = apply_semantic_key_mode(samples, semantic_key_mode)
+    samples = apply_semantic_key_mode(
+        samples,
+        semantic_key_mode,
+        path_richness_source=semantic_key_path_richness_source,
+    )
     samples = filter_samples_by_attribute_values(
         samples,
         implied_attribute_value_filters(attribute_fields, attribute_remap),
@@ -580,6 +587,7 @@ def build_real_components(
     temperature: float = 0.07,
     min_class_size: int = 1,
     semantic_key_mode: str = "full",
+    semantic_key_path_richness_source: str = DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     attribute_fields: tuple[str, ...] = (),
     attribute_remap: dict[str, dict[str, tuple[str, ...]]] | None = None,
     filter_attribute_values: dict[str, tuple[str, ...]] | None = None,
@@ -589,7 +597,11 @@ def build_real_components(
     tokenizer_word2id: dict[str, int] | None = None,
 ):
     dataset = PreprocessedCSIDataset.from_pt(data_path)
-    mode_samples = apply_semantic_key_mode(dataset.samples, semantic_key_mode)
+    mode_samples = apply_semantic_key_mode(
+        dataset.samples,
+        semantic_key_mode,
+        path_richness_source=semantic_key_path_richness_source,
+    )
     if semantic_key_mode != "full":
         before_counts = Counter(sample.semantic_key for sample in dataset.samples)
         after_counts = Counter(sample.semantic_key for sample in mode_samples)
@@ -619,7 +631,7 @@ def build_real_components(
                 f"{value}:{count}"
                 for value, count in sorted(
                     Counter(
-                        semantic_key_attribute_raw_value(sample.semantic_key, field)
+                        semantic_sample_attribute_raw_value(sample, field)
                         for sample in value_filtered_samples
                     ).items()
                 )
@@ -644,8 +656,8 @@ def build_real_components(
         before_counts = Counter(sample.semantic_key for sample in samples)
         after_counts = Counter(sample.semantic_key for sample in balanced_limited_samples)
         value_counts = Counter(
-            semantic_key_attribute_value(
-                sample.semantic_key,
+            semantic_sample_attribute_value(
+                sample,
                 limit_samples_by_attribute,
                 attribute_remap,
             )
@@ -701,11 +713,13 @@ def run_smoke_test(
     multipositive_positive_mode: str = "semantic_and_physics",
     min_class_size_for_multipositive: int = 2,
     semantic_key_mode: str = "full",
+    semantic_key_path_richness_source: str = DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
     attribute_remap: dict[str, dict[str, tuple[str, ...]]] | None = None,
 ) -> None:
     loader, model, _, prototype_bank = build_demo_components(
         device,
         semantic_key_mode=semantic_key_mode,
+        semantic_key_path_richness_source=semantic_key_path_richness_source,
         attribute_fields=attribute_classifier_fields,
         attribute_remap=attribute_remap,
     )
@@ -791,6 +805,7 @@ def run_real_pretrain(
     min_class_size_for_multipositive: int,
     min_class_size: int,
     semantic_key_mode: str,
+    semantic_key_path_richness_source: str,
     attribute_remap: dict[str, dict[str, tuple[str, ...]]],
     filter_attribute_values: dict[str, tuple[str, ...]],
     limit_samples: int | None,
@@ -813,6 +828,7 @@ def run_real_pretrain(
         temperature=temperature,
         min_class_size=min_class_size,
         semantic_key_mode=semantic_key_mode,
+        semantic_key_path_richness_source=semantic_key_path_richness_source,
         attribute_fields=attribute_classifier_fields,
         attribute_remap=attribute_remap,
         filter_attribute_values=effective_filter_attribute_values,
@@ -916,6 +932,7 @@ def run_real_pretrain(
         f"multipositive_positive_mode={multipositive_positive_mode} "
         f"min_class_size_for_multipositive={min_class_size_for_multipositive} "
         f"min_class_size={min_class_size} semantic_key_mode={semantic_key_mode} "
+        f"semantic_key_path_richness_source={semantic_key_path_richness_source} "
         f"filter_attribute_values={format_attribute_value_filters(effective_filter_attribute_values)} "
         f"limit_samples={limit_samples} "
         f"limit_samples_by_attribute={limit_samples_by_attribute} "
@@ -1084,6 +1101,7 @@ def run_real_pretrain(
                         "min_class_size_for_multipositive": min_class_size_for_multipositive,
                         "min_class_size": min_class_size,
                         "semantic_key_mode": semantic_key_mode,
+                        "semantic_key_path_richness_source": semantic_key_path_richness_source,
                         "filter_attribute_values": {
                             field: list(values)
                             for field, values in effective_filter_attribute_values.items()
@@ -1144,6 +1162,7 @@ def run_real_pretrain(
                     "min_class_size_for_multipositive": min_class_size_for_multipositive,
                     "min_class_size": min_class_size,
                     "semantic_key_mode": semantic_key_mode,
+                    "semantic_key_path_richness_source": semantic_key_path_richness_source,
                     "filter_attribute_values": {
                         field: list(values)
                         for field, values in effective_filter_attribute_values.items()
@@ -1230,6 +1249,11 @@ def main() -> None:
             "Try small values such as 0.1 or 0.25; defaults to config or 0.0."
         ),
     )
+    parser.add_argument(
+        "--semantic-key-path-richness-source",
+        choices=semantic_path_richness_source_choices(),
+        help="Which path_richness source to use when remapping semantic_key_mode prototypes.",
+    )
     parser.add_argument("--aux-regression-weight", type=float)
     parser.add_argument(
         "--aux-regression-targets",
@@ -1333,6 +1357,17 @@ def main() -> None:
         if args.semantic_key_mode is not None
         else str(cfg_get(train_cfg, "semantic_key_mode", "full"))
     )
+    semantic_key_path_richness_source = (
+        args.semantic_key_path_richness_source
+        if args.semantic_key_path_richness_source is not None
+        else str(
+            cfg_get(
+                train_cfg,
+                "semantic_key_path_richness_source",
+                DEFAULT_SEMANTIC_PATH_RICHNESS_SOURCE,
+            )
+        )
+    )
     semantic_classifier_weight = (
         args.semantic_classifier_weight
         if args.semantic_classifier_weight is not None
@@ -1459,6 +1494,7 @@ def main() -> None:
             multipositive_positive_mode=multipositive_positive_mode,
             min_class_size_for_multipositive=min_class_size_for_multipositive,
             semantic_key_mode=semantic_key_mode,
+            semantic_key_path_richness_source=semantic_key_path_richness_source,
         )
         return
 
@@ -1495,6 +1531,7 @@ def main() -> None:
             min_class_size_for_multipositive=min_class_size_for_multipositive,
             min_class_size=min_class_size,
             semantic_key_mode=semantic_key_mode,
+            semantic_key_path_richness_source=semantic_key_path_richness_source,
             attribute_remap=attribute_remap,
             filter_attribute_values=filter_attribute_values,
             limit_samples=limit_samples,
diff --git a/training/trainer.py b/training/trainer.py
index 61d466c..5cebac1 100644
--- a/training/trainer.py
+++ b/training/trainer.py
@@ -5,7 +5,10 @@ import math
 
 import torch
 
-from data.semantic_key import AttributeRemap, semantic_key_attribute_value
+from data.semantic_key import (
+    AttributeRemap,
+    semantic_key_attribute_value_from_sources,
+)
 
 from .losses import (
     PrototypeClipLoss,
@@ -85,13 +88,26 @@ class Trainer:
         self.attribute_remap = attribute_remap or {}
         self.loss = loss or PrototypeClipLoss()
 
-    def _attribute_targets(self, semantic_keys: list[object]) -> dict[str, torch.Tensor]:
+    def _attribute_targets(
+        self,
+        semantic_keys: list[object],
+        aux_semantic_keys: list[object] | None = None,
+    ) -> dict[str, torch.Tensor]:
         targets = {}
+        if aux_semantic_keys is None:
+            aux_semantic_keys = [None] * len(semantic_keys)
         for field, label_map in self.attribute_label_maps.items():
             targets[field] = torch.tensor(
                 [
-                    label_map[semantic_key_attribute_value(key, field, self.attribute_remap)]
-                    for key in semantic_keys
+                    label_map[
+                        semantic_key_attribute_value_from_sources(
+                            key,
+                            field,
+                            attribute_remap=self.attribute_remap,
+                            aux_key=aux_key,
+                        )
+                    ]
+                    for key, aux_key in zip(semantic_keys, aux_semantic_keys)
                 ],
                 device=self.device,
                 dtype=torch.long,
@@ -387,7 +403,10 @@ class Trainer:
             losses["logit_std_semantic_classifier"] = semantic_logits.detach().float().std()
         if effective_attribute_classifier_weight > 0:
             attribute_logits = self.model.predict_attributes(csi_features_raw)
-            attribute_targets = self._attribute_targets(batch["semantic_keys"])
+            attribute_targets = self._attribute_targets(
+                batch["semantic_keys"],
+                batch.get("aux_semantic_keys"),
+            )
             attribute_losses = []
             attribute_accuracies = []
             attribute_logit_stds = []
